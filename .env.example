# Modo público (true/false)
# Si es true, la app se abre sin autenticación (solo para demos).
# En producción mantenlo en false para que esté protegido por código.
VITE_PUBLIC_MODE=false

# Código de acceso requerido
# Usa un valor de 6 dígitos. Configúralo en tu entorno (local o plataforma de deploy).
# NUNCA subas tu .env con secretos al repositorio.
VITE_ACCESS_CODE=246810
