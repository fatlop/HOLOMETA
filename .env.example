# Modo público (true/false). Si es true, salta la autenticación.
VITE_PUBLIC_MODE=false

# Código de acceso requerido en el paso 2 de autenticación
# Cambia este valor en tu .env local (NO lo subas al repo)
VITE_ACCESS_CODE=246810
